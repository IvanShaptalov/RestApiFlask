[DATABASE]
URL = production database name, example : production.sqlite3
TEST_URL = database test url, note that this url must not be same as production database url
DRIVER = database driver, example : sqlite:///
[KEYS]
SECRET_KEY = your secret key
[JWT]
LIVE_MINUTES = json web tokens live in minutes
